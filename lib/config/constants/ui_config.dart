import 'package:flutter/material.dart';

class UiConfig {
  UiConfig._();

  static const Size designSize = Size(393, 852);

  static const double appLogoFactor = 0.65;
  static const double teamLogoSize = 50;
}
